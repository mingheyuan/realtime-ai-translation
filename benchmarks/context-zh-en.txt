这是公开的合成性能测试背景，不包含用户数据。产品名称固定写作 Realtime Translation，模型名称固定写作 DeepSeek，语音识别缩写为 ASR，快速翻译模型为 MarianMT。DeepSeek 不应写成 Deep C、Deep Seek 或其他近音形式。系统链路由 Sherpa-ONNX、MarianMT 和 DeepSeek 组成。指标包括 ASR 启动时间、首段文字、partial 更新间隔、已显示前缀回改率、断句等待、短碎片率、MarianMT 推理时间、草稿就绪时间、LLM 请求延迟、请求字符数、原文修订率、译文修订率和终稿端到端时间。参考背景只能用于术语、实体、缩写和领域消歧，不能补入语音没有表达的事实。正式对比必须使用同一段音频、相同语言方向、相同 ASR、相同音频来源、相同背景和相同模型配置。冷启动与预热场景分开记录，至少重复三次并采用中位数。阶段二将把完整背景切分为可检索片段，只选择与当前语音相关的少量内容。主指标是 LLM 请求字符数和请求延迟，护栏是实体命中、用户纠正率和终稿端到端延迟。测试语句中的问题、否定、数字和专有名词都必须保留。测试编号二零二六对应数字 2026。固定术语再次确认：DeepSeek、Realtime Translation、Sherpa-ONNX、MarianMT、ASR、LLM、WebSocket、SQLite、macOS、Rust、Swift、Python。
