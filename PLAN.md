# Realtime AI Translation 实施计划

本计划按依赖顺序推进。每个阶段必须通过自动化测试、实际运行验收并独立提交后，才进入下一阶段。

## 阶段 1：LLM 同时纠正 ASR 原文与最终译文（已完成）

- 将 LLM 输出改为结构化的 `corrected_source` 与 `translation`。
- 允许词典、最近上下文和参考背景纠正同音词、实体名及中英混读错误。
- 对原文改写增加长度与编辑距离保护，拒绝激进重写。
- 在当前字幕封存前一次性回写纠正后的原文和译文。
- 验收：旧的纯译文响应可安全降级；迟到结果不能修改已经封存的其他段。

## 阶段 2：参考背景检索与缓存

- 将直接文字和文档内容切分为可检索片段。
- 根据当前 ASR 原文选择最相关的少量片段，而不是每次发送全部内容。
- 保留术语、标题和表格行结构，并为固定背景准备可缓存前缀。
- 验收：上下文输入显著缩短，相关术语命中不下降。

## 阶段 3：延迟与准确性指标

- 记录 ASR 首字、partial 间隔、断句、MarianMT 和 LLM 延迟。
- 记录 ASR 修订次数、LLM 原文修订量和用户最终纠正率。
- 在主界面提供轻量诊断视图，支持 Apple Speech 与 Sherpa-ONNX 对比。
- 验收：可以定位每个阶段的延迟，并导出一次会话摘要。

## 阶段 4：自适应断句与 VAD

- 区分短暂停顿、犹豫停顿和真正结束。
- 根据语速、稳定标点、静音长度和最大句长动态决定封存。
- 避免一秒一断，同时为长句设置明确上限。
- 验收：短碎片显著减少，停止和最终封存不再卡住。

## 阶段 5：可选二次高精度 ASR

- 保留 Sherpa-ONNX 负责低延迟 partial。
- 对已经断句的音频片段按需使用高精度 ASR 生成终稿。
- 终稿变化触发译文回滚和重算，但不阻塞首字显示。
- 验收：可关闭、可替换、按需加载，且不破坏单 ASR 模式。

## 阶段 6：模型与进程生命周期

- 模型安装后默认离线加载，避免启动时远程检查。
- 为 MarianMT、Sherpa 和音频桥增加明确停止协议与超时清理。
- 增加子进程崩溃检测、有限重启和可理解的错误状态。
- 验收：启动、停止和重启无残留进程、无 `KeyboardInterrupt` 噪声。

## 阶段 7：设置持久化

- 保存 ASR、音频来源、语言方向、断句灵敏度和 LLM 背景设置。
- 保存悬浮窗位置、大小和折叠状态。
- 对文档路径失效、敏感内容和版本迁移提供安全降级。
- 验收：服务重启后恢复设置，失效配置不会阻止应用启动。

## 阶段 8：原生 macOS 应用打包

- 将服务、菜单栏控制、悬浮字幕和模型状态整合为一个 `.app`。
- 支持一键启动、正常退出、模型安装检查和崩溃恢复。
- 完成稳定签名、权限说明和发布构建流程。
- 验收：无需终端即可完成安装、授权、启动、翻译和退出。
